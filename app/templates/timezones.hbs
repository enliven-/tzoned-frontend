{{outlet}}

<h3>Your timezones</h3>
{{#each model as |timezone|}}
  <div class="well">
    {{#link-to "timezones.edit" timezone.id}} {{timezone.abbr}} {{/link-to}}
    <p>{{timezone.name}} : GMT + {{timezone.gmt_difference}}</p>
  </div>
{{/each}}

